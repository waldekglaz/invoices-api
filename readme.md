# Starting template for Node js with Typescript and express

This is a starting template

## Setup

1. **Clone the repository:**

   ```bash
   git clone <repository-url>
   cd node-ts-starting-template

   ```

2. **Run Project:**

```bash
 npm install
 npm run dev
```
